# auto-generated by grc.converter

id: tnc_nx_local_extrema_detect
label: Local Extrema Detector
category: TNC NX

parameters:
-   id: hist
    label: Samples Analyzed
    dtype: int
    default: '2'
-   id: threshold
    label: Slope threshold
    dtype: float
    default: '0.02'
-   id: range
    label: Output amplitude
    dtype: float
    default: '0.1'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import tnc_nx
    make: tnc_nx.local_extrema_detect(${hist}, ${threshold}, ${range})
    callbacks:
    - set_hist(${hist})
    - set_threshold(${threshold})
    - set_range(${range})

file_format: 1
