# auto-generated by grc.converter

id: tnc_nx_signal_demodulator
label: GMSK Demodulator
category: TNC NX

parameters:
-   id: dc_block_len
    label: DC Blocker Length
    dtype: raw
    default: '1000'
-   id: samp_per_sym
    label: Sample per Symbol
    dtype: raw
    default: '10'
-   id: rx_bit_inv
    label: FM Sensitivity
    dtype: raw
    default: '1'

inputs:
-   domain: stream
    dtype: float
    vlen: 1

outputs:
-   domain: stream
    dtype: byte
    vlen: 1

templates:
    imports: from gnuradio import tnc_nx
    make: "tnc_nx.signal_demodulator(\n\tdc_block_len=${dc_block_len},\n\tsamp_per_sym=${samp_per_sym},\n\
        \trx_bit_inv=${rx_bit_inv},\n)"
    callbacks:
    - set_dc_block_len(${dc_block_len})
    - set_samp_per_sym(${samp_per_sym})
    - set_rx_bit_inv(${rx_bit_inv})

documentation: |-
    Provides GMSK demodulator with DC-blocked input stream.

file_format: 1
