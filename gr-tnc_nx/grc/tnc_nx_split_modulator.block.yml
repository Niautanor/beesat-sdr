# auto-generated by grc.converter

id: tnc_nx_split_modulator
label: Split GMSK Modulator
category: TNC NX

parameters:
-   id: gain
    label: FM Gain
    dtype: raw
    default: '1'

inputs:
-   domain: message
    id: 4k8
    optional: true
-   domain: message
    id: 9k6
    optional: true

outputs:
-   label: mux out
    domain: stream
    dtype: float
    vlen: 1

templates:
    imports: from gnuradio import tnc_nx
    make: tnc_nx.split_modulator(gain=${gain})
    callbacks:
    - set_gain(${gain})

documentation: |-
    Provides simple and double speed GMSK modulator paths and adds them to a single output stream.

file_format: 1
